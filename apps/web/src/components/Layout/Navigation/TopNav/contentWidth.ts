/**
 * Shared max-width and padding for TopNav, main content, and Footer
 * so they align across the layout.
 */
export const CONTENT_WIDTH_CLASS =
  'mx-auto w-full max-w-[clamp(1024px,calc(1024px+(100vw-1024px)*0.25),1248px)] px-4 md:px-6 lg:px-8';
